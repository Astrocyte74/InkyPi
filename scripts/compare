#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/.." && pwd)"

exec /usr/local/inkypi/venv_inkypi/bin/python "${REPO_ROOT}/scripts/compare_ai_prompts.py" --interactive "$@"
